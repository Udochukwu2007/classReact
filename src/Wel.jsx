import React from "react"
function Wel({name}) {
    return (
        <div>
            <h1 className="on">Welcome back {name}</h1>
            <button onClick={coco} className="btn">Click me</button>
        </div>
    )
}

export default Wel